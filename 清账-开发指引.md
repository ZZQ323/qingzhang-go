# 清账 · 开发指引

> 一份自用的开发指引。后端 Go + SQLite 已搭好骨架，本文档列清目标、待办任务（按依赖顺序）、以及贯穿全程的开发规范，方便持续开发与协作。

---

## 一、项目目标

做一个**自己和 2–3 个好友用的、无广告的云同步记账小程序**，跑在一台 2 核 2G 服务器上，与已有的 VitePress 博客、frpc 共存。

### 核心原则
- **轻**：后端单个 Go 静态二进制 + 一个 SQLite 文件，常驻内存 50–80MB。零中间件（无 Redis/Nacos/Kafka/MySQL）。
- **离线优先**：记一笔先落本地、秒回、无网也能记；联网后自动同步。
- **数据安全**：数据虽上云，仍保留本地缓存与导出能力，多一层保险。
- **够用即止**：2–3 人规模，不做多租户、不做复杂权限、不过度设计。

### 已完成（骨架）
- 后端三接口：`POST /api/auth/login`、`GET /api/sync/pull`、`POST /api/sync/push`
- JWT 鉴权、微信 code 换 openid、SQLite（WAL + 单写连接）、建表迁移
- 前端：`api/request.js`（请求封装）、`store/ledger.js`（离线优先 + 增量同步的 Pinia store）

### 尚未完成（本文档的待办）
- 前端所有页面（记账、明细、统计、我的）
- 真实微信登录打通
- 共享账本
- 微信账单 CSV 导入（v2）

---

## 二、任务清单

按依赖顺序排列。建议自上而下做，每完成一项过一遍「验收」再进入下一项。

### 阶段 0 · 跑通后端（半天）

- [ ] **0.1** 本机装 Go 1.22+，项目根目录执行 `go mod tidy` 拉依赖
- [ ] **0.2** 设环境变量后 `go run .`，确认自动生成 `qingzhang.db` 且监听 8080
      - 验收：`curl localhost:8080/api/sync/pull` 返回 401（因为没带 token，符合预期）
- [ ] **0.3** 加一个 `DEV_MODE` 开关：开发态跳过微信换 openid，用固定 openid 直接签 token
      - 目的：前端还没接 `wx.login` 时也能调 pull/push
      - 验收：`DEV_MODE=1` 时 `POST /api/auth/login` 传任意 code 都能拿到 token
- [ ] **0.4** 用 token 跑通 push → pull 闭环（curl 或 Postman 各一条）
      - 验收：push 一条记录后，pull 能拉回它，且金额是「分」

### 阶段 1 · 前端骨架与登录（1–2 天）

- [ ] **1.1** 初始化 uniapp + Vue3 + Pinia 项目，接入已有的 `request.js` 和 `ledger.js`
- [ ] **1.2** `manifest.json` 填小程序 AppID；`request.js` 的 `BASE` 指向本机或测试域名
- [ ] **1.3** 实现登录：`wx.login` 拿 code → 调 `/api/auth/login` → 存 token 到 storage
      - 验收：冷启动后自动静默登录，token 持久化，重开不需重登
- [ ] **1.4** App 启动时调用 `ledgerStore.init()`（先 push 待发队列，再 pull 增量）
      - 验收：杀进程重开，数据从服务端恢复

### 阶段 2 · 记账核心（2–3 天）

- [ ] **2.1** 记账面板（底部弹出 sheet）：金额、收/支切换、类别选择、备注、日期、账户、收支人员
      - 把之前的 HTML 交互稿转成 Vue 组件；金额输入后 **×100 存为分**
      - 验收：填完保存，记录立即出现在明细列表（本地秒回），后台异步 push
- [ ] **2.2** 明细列表：按日期分组、当日收支小计、点击进入编辑
      - 验收：无网状态下也能记账与查看；来网后自动补传
- [ ] **2.3** 编辑 / 删除：编辑复用记账面板；删除走软删（`isDeleted=1` 后 upsert）
      - 验收：删除的记录在本地与服务端都不再展示，但 pull 能看到软删标记
- [ ] **2.4** 顶部结余卡：本月收入、支出、结余（注意 **分 ÷100** 展示）

### 阶段 3 · 统计（1–2 天）

- [ ] **3.1** 收/支构成饼图（按类别聚合当月数据）
- [ ] **3.2** 近 6 个月收支柱状图
- [ ] **3.3** 时间筛选（本月 / 上月 / 自定义区间）
      - 验收：切换区间，饼图与列表同步刷新

### 阶段 4 · 数据保险（半天）

- [ ] **4.1** 前端「导出 CSV」入口（本地数据导出，断网也能用）
- [ ] **4.2** 服务器加每日 SQLite 备份 crontab：
      `sqlite3 /opt/qingzhang/qingzhang.db ".backup '/backup/qingzhang-$(date +\%F).db'"`
      - 验收：第二天 `/backup` 下出现带日期的备份文件

### 阶段 5 · 共享账本（1–2 天，可选）

- [ ] **5.1** 后端加「加入账本」接口：把当前用户的 `book_id` 改成目标账本 id
      - 最简方案：账本主人把自己的 `book_id`（即其 user id）作为邀请码分享，朋友输入后加入
- [ ] **5.2** 前端加「我的 → 账本设置」：显示邀请码、输入邀请码加入
      - 验收：两个账号加入同一 book_id 后，互相能看到对方记的账
- [ ] **5.3** 明细列表区分记账人（共享账本下显示是谁记的）

### 阶段 6 · 微信账单导入（v2，可选）

- [ ] **6.1** 后端加 `POST /api/import/wxbill` 接口，接收上传的微信账单 CSV
      - 复用之前给的 `WxBillImporter` 解析逻辑（已用 Go 思路描述，需移植）
      - 解析后批量 upsert，类别做粗映射，导入后用户可再改
- [ ] **6.2** 前端加导入入口 + 导入预览（让用户确认后再入库）
      - 验收：上传真实微信账单 CSV，能正确解析出支出/收入并入账
- [ ] 注意：微信**无个人账单 API**，只能走「用户手动导出 CSV → 上传」的半自动路径

### 上线检查清单

- [ ] `JWT_SECRET` 已换成 32 字节以上强随机串（非默认值）
- [ ] SQLite 文件权限收紧，nginx 无法直接读取 `.db`
- [ ] MySQL/SQLite 不对公网开放（SQLite 本就是本地文件，确认无目录遍历）
- [ ] HTTPS 证书就绪，域名在小程序后台配为合法 request 域名
- [ ] 关闭 `DEV_MODE`
- [ ] 备份 crontab 已生效并验证过可恢复

---

## 三、开发规范

### 通用
- **金额一律存「分」（int64）**。前端展示 ÷100，输入 ×100。后端、数据库、传输全程是分，绝不出现浮点金额。
- **时间统一 UTC 存储**，格式 `YYYY-MM-DD HH:MM:SS`；账目发生日期用 `YYYY-MM-DD`。展示时前端按本地时区转。
- **软删不硬删**：删除即 `isDeleted=1` 后 upsert，保证增量同步能把删除同步到其他设备。
- **接口契约不轻易改**。前后端约定的 `{code,msg,data}` 与字段名（驼峰）保持稳定；要改先两端同步。

### 后端（Go）
- 包结构维持现状：`internal/{db,handler,middleware,wx}`，入口 `main.go`。
- **SQLite 写串行**：保持 `db.SetMaxOpenConns(1)` 与连接串的 `WAL + busy_timeout`，不要随意加大连接池。
- 多条写操作放进一个事务（参考 `Upsert`），别在循环里开多个事务。
- 错误处理：handler 层捕获后用 `writeErr` 返回友好 msg，不要把原始 error 直接暴露给客户端的同时也别吞掉日志。
- 新增接口：公开接口注册在 `main.go` 的 mux，鉴权接口用 `auth(...)` 包裹。
- 编译产物用 `CGO_ENABLED=0 GOOS=linux go build`，保证零依赖静态二进制。
- 依赖能不加就不加；标准库能搞定的（路由、JSON、HTTP 客户端）不引第三方。

### 前端（uniapp + Vue3 + Pinia）
- **所有数据变更走 store**（`upsert` / `remove`），组件不直接碰 storage 或发请求。
- 记账操作必须**先写本地、立即反映到 UI**，再后台 push；不要让用户等网络。
- 网络失败要静默容错（队列保留，下次重试），不要弹错误打断记账。
- 请求统一走 `api/request.js`；新接口在里面加方法，不在组件里散落 `uni.request`。
- 组件保持轻：页面只负责展示与交互，聚合/统计逻辑放 store 的 getter 或 utils。
- 样式沿用已有 HTML 稿的视觉（深色、金色强调），保持一致。

### Git / 协作
- 分支：`main` 保持可部署；功能在 `feat/xxx` 分支开发，自测通过再合。
- commit message 写清做了什么（如 `feat: 记账面板与本地秒回`），方便回溯。
- 敏感信息（AppID、Secret、JWT_SECRET）走环境变量，**绝不提交进仓库**；加 `.gitignore` 排除 `*.db`、`qingzhang` 二进制、本地 env 文件。

### 测试建议（够用即可，不必追求覆盖率）
- 后端：对 `db` 层的 `Upsert`/`Pull` 写几个单元测试（用临时 db 文件），覆盖新增、更新、软删、增量拉取四种情况。
- 同步逻辑：手动测「断网记账 → 联网自动补传」「两设备先后修改同一条」两个关键场景。

---

## 四、推进建议

不必严格按阶段串行，但**阶段 0、1、2 是地基，务必先打牢**。统计和共享账本是体验增强，账单导入是锦上添花，按你的兴趣和时间排。

每做完一个阶段，建议在服务器上部署一次、用真机走一遍，比在本地反复调更早暴露问题（尤其是 HTTPS、域名白名单、真机网络这些只有上真环境才会遇到的坑）。
